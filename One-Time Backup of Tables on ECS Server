**************** Request for One-Time Backup of Tables on ECS Server **************
Server ip :- 192.168.41.39
Database:- sms_email_sending

######## steps ###########
go to server ip 
make a file  
cat it and remove spaces
need to take dump (backup of tables)
for i in cat one_time_backup; do echo $i; sudo mysqldump sms_email_sending $i | gzip -fv > $i.sql.gz; done
check file size  (make sure partition has enough space for backup)
Reply the mail including the path 


